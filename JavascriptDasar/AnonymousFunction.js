/* 
  Anonymous function = function tanpa nama
  function tanpa nama biasanya dijadikan value atau disimpan di variable
*/

const greeting = function (name) {
  return `Hello ${name}!`;
};

console.log(greeting('Yelly'));
